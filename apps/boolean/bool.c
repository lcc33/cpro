#include <stdio.h>
#include <stdbool.h>

int main()
{
  bool isVerified = true;
  bool isActive = false;
  bool isMale = true;

  printf("Is the user verified? %d\n", isVerified);
  printf("Is the user active? %d\n", isActive);
  printf("Is the user a male? %d\n", isMale);

  return 0;
}